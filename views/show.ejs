<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Souljet</title>
    <style>
        body{
            background-image: linear-gradient(rgba(4,9,30,0.1),rgba(4,9,30,0.1));
            background-position: center;
            background-size: cover;
            position: relative;
        }
        form{
            margin: 80px auto 80px;
            height: auto;
            width: 60%;
            padding: 10px;
            background-color: ghostwhite;
            border-radius: 15px;
        }

        .username, .email, .password{
            text-align: center;
            font-size: 1em;
            font-weight: bold;
            padding: 10px;
            border-radius: 5px;
            margin-top: 40px;
        }

        .submitButton{
            margin: 10px;
            padding:2px 6px 2px 6px;
            text-align: center;
            background-color: #ecb21f;
            border-color: #a88734 #9c7e31 #846a29;
            font-size: large;
            color: black;
            border-width: 1px;
            border-style: solid;
            border-radius: 13px;
            width: 5em;
        }
    </style>
</head>
<body>
<form action="/user/edit/<%= user._id %>" method="POST">
    <center>
    <input name="name" value="<%= user.username %>" class="username">
    <input name="email" value="<%= user.email %>" class="email">
    <input name="password" value="<%= user.password %>" class="password">
    <br><br><input type="submit" value="save" class="submitButton">
    </center>
</form>
</body>
</html>